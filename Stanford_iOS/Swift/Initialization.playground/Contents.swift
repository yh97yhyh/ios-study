var greeting = "Hello, playground"


/*
 Initialization
 
 - init을 하나 이상 만들었다면 자동 생성되는 init을 호출하지 마라
 - init이 끝나면 모든 property는 값을 가져야 함 (optional 포함)
 - super class를 init하기 전에 모든 property 초기화
 - super class의 어떤 property를 건들기 전에 super class의 init을 호출
 
 
 - required init
 child class가 이 무조건 Init을 상속해야 한다
 
 */
