import UIKit

var greeting = "Hello, playground"

/*
 class, struct, enum
 
 공통점
 - 같은 방식으로 선언됨
 - 모두 property와 function을 가질 수 있음 (enum은 저장 property를 가질 수 없음)
 - initializer를 갖고 있음
 
 다른점
 - class만 상속 가능
 - struct, enum : pass by value
 
 * swift는 heap으로 메모리 관리 (weak, strong)
 
 Choosing which to use?
 - Usually you will choose class over struct
 */
